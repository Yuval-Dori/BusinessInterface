@model IEnumerable<BusinessesInterface.Models.DeviceUsage>

@{
    ViewBag.Title = "List of Items";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Usage History</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DeviceId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HardwareId) //remove
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CurrentLocation) //remove
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TimeStamp)
        </th>
        <th></th>
    </tr>

    @foreach (var usage in Model)
    {
<tr>
    <td>
        @Html.DisplayFor(modelItem => usage.DeviceId)
    </td>
    <td>
        @Html.DisplayFor(modelItem => usage.HardwareId) //remove
    </td>
    <td>
        @Html.DisplayFor(modelItem => usage.CurrentLocation) //remove
    </td>
    <td>
        @Html.DisplayFor(modelItem => usage.TimeStamp)
    </td>
    <td> //remove all these
        @Html.ActionLink("Edit", "Edit", new { id = usage.Id }) |
        @Html.ActionLink("Details", "Details", new { id = usage.Id }) |
        @Html.ActionLink("Delete", "Delete", new { id = usage.Id })
    </td>
</tr>
    }

</table>

<p> //remove
    @Html.ActionLink("Create New", "Create")
</p>